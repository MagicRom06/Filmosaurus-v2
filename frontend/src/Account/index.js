import axios from 'axios';

const Account = ({token}) => {

    const handleLoadAccount = () => {
        console.log('loading');
    }

    return (
        <h3>Account</h3>
    )
}

export default Account;